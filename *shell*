bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

hello
aaa
hello
aaa
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

0It is not Black's turn to move.

There is no piece at position D4!

hello
aaa
hello
aaa
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

hello
aaa
hello
aaa
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!


Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

0It is not Black's turn to move.

There is no piece at position D4!

White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

hello0It is not Black's turn to move.

There is no piece at position D4!

White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

hello0It is not Black's turn to move.

There is no piece at position D4!

White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

hellocheck1It is not Black's turn to move.

There is no piece at position D4!

White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

check1White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!


It is not Black's turn to move.

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
White's Pawn can't moves to D4

Black's Bishop can't moves to B4

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1

It is not Black's turn to move.

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1

It is not Black's turn to move.

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1

It is not Black's turn to move.

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1

It is not Black's turn to move.

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1

It is not Black's turn to move.

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1

It is not Black's turn to move.

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

Segmentation fault
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1

It is not Black's turn to move.

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

Segmentation fault
bw222@texel08:msc_lab3_bw222$ gdb
GNU gdb (Ubuntu 12.0.90-0ubuntu1) 12.0.90
Copyright (C) 2022 Free Software Foundation, Inc.
License GPLv3+: GNU GPL version 3 or later <http://gnu.org/licenses/gpl.html>
This is free software: you are free to change and redistribute it.
There is NO WARRANTY, to the extent permitted by law.
Type "show copying" and "show warranty" for details.
This GDB was configured as "x86_64-linux-gnu".
Type "show configuration" for configuration details.
For bug reporting instructions, please see:
<https://www.gnu.org/software/gdb/bugs/>.
Find the GDB manual and other documentation resources online at:
    <http://www.gnu.org/software/gdb/documentation/>.

For help, type "help".
Type "apropos word" to search for commands related to "word".
(gdb) break main
No symbol table is loaded.  Use the "file" command.
Make breakpoint pending on future shared library load? (y or [n]) y
Breakpoint 1 (main) pending.
(gdb) r
Starting program:  
No executable file specified.
Use the "file" or "exec-file" command.
(gdb) n
The program is not being run.
(gdb) s
The program is not being run.
(gdb) finish
The program is not being run.
(gdb) backtrace
No stack.
(gdb) r
Starting program:  
No executable file specified.
Use the "file" or "exec-file" command.
(gdb) s
The program is not being run.
(gdb) s
The program is not being run.
(gdb) s
The program is not being run.
(gdb) break main
No symbol table is loaded.  Use the "file" command.
Make breakpoint pending on future shared library load? (y or [n]) y
Breakpoint 2 (main) pending.
(gdb) ./chess
Undefined command: ".".  Try "help".
(gdb) exit
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1

It is not Black's turn to move.

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ clear
bw222@texel08:msc_lab3_bw222$ ./cheww
bash: ./cheww: No such file or directory
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1

It is not Black's turn to move.

=========================
Alekhine vs. Vasic (1931)
=========================

A new chess game is started

bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$./chess 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ 
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.
x
There is no piece at position D4!

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================x
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

check1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ ./chess
========================
Testing the Chess Engine
========================

A new chess game is started

It is not Black's turn to move.

There is no piece at position D4!

1
Segmentation fault
bw222@texel08:msc_lab3_bw222$ 